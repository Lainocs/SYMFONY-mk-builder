<div>

    {{ form_start(form) }}
    {{ form_widget(form) }}
        <button class="btn btn-success mt-3" type="submit">
            <span class="fa fa-save"></span>
            Enregistrer
    {{ form_end(form) }}

</div>
